import React from 'react';

import MapChart from '../Charts/mapChart';

export const Events = () => {
    return (
        <div>
            <MapChart />
        </div>
    )
}

export default Events;